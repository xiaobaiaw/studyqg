<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.guanghui.amen.mapper.OrdersMapper">
    <select id="findPage" resultType="com.guanghui.amen.entity.Orders">
        select o.*, u.username, u.nickname from orders o left join sys_user u on o.user_id = u.id
        <where>
            <if test="name != ''">
                and o.name like concat('%',#{name},'%%')
            </if>
            <if test="role == 'ROLE_USER' || role == 'ROLE_TEACHER' || role == 'ROLE_STUDENT'">
                and o.user_id = #{userId}
            </if>
        </where>
    </select>
</mapper>